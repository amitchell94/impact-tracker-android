<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codingnomads.impacttracker">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        >
        <activity android:name=".presentation.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ImpactActivity">
        </activity>
        <activity
            android:name=".presentation.LoginActivity">
        </activity>
        <activity
            android:name=".presentation.RegisterActivity">
        </activity>
        <activity
            android:name=".presentation.CommitmentsActivity">
        </activity>
        <activity
            android:name=".presentation.AddCommitmentsActivity">
        </activity>
    </application>

</manifest>